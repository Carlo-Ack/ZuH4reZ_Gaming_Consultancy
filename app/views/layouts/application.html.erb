<!DOCTYPE html>
<html>
  <head>
    <title>ZuH4reZGamingConsultancy</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <%= render "shared/usernavbar" %>
    <%= render "shared/userbanner" if user_signed_in? %>

    <%= yield %>
  
    <%= render "shared/userfooter" if user_signed_in? %>
  
  </body>
</html>
