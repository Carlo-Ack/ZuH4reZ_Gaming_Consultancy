<h1> New Consultation </h1>

<h2>Choose a Category:</h2>
<%= form_with url: "/consult/category", method: :post do %>
  <%= select_tag :category, options_for_select(@categories, @consult_category) %>
  <%= submit_tag "Start Survey" %>
<% end %>

<%# depending on the chosen category have a survey prepared
<%# Should be hidden until Start Survey was submitted %>

<% if @consult_category == "Shooter" %>
  <%= render 'shared/survey_shooter' %>
<% elsif @consult_category == "RPG" %>
  <%= render 'shared/survey_rpg' %>
<% end %>
<%# This then for all the other categories! %>

<%# 4. get consulted by LLM? %>
<%# open chat? %>

<%# 5. get consulted by ZuH4reZ? %>
<%# open chat with admin? %>
