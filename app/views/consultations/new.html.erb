<h1> New Consultation </h1>

<h2>Choose a Category:</h2>
<%= form_with url: "/consult/category", method: :post do %>
  <%= select_tag :category, options_for_select(@categories) %>
  <%= submit_tag "Start Survey" %>
<% end %>

<%# 2.depending on the chosen category have a survey prepared
<%# if @consult_category == "Shooter" do ?render? @shooter_survey %>

<h3>Shooter Survey</h3>
<%= form_with url: "/consult/submit_survey", method: :post do %>
  <%= hidden_field_tag :consult_category, "Shooter" %>

<%# 3. filling in survey (what do you like about <category?>, what would you like to do in the <category>) %>
<%# render shared form <category> with input forms with placeholders %>

  <p>What do you like about Shooter games?</p>
  <%= text_area_tag "responses[likes]", nil, placeholder: "Fast action, aiming skills, tactial squad coordination..." %>
  <p>How difficult should the Shooter game be?</p>
  <%= text_area_tag "responses[difficulty]", nil, placeholder: "Sweaty, casual-friendly, easy..." %>
  <%= submit_tag "Submit Survey" %>
<% end %>



<%# 4. get consulted by LLM? %>
<%# open chat? %>

<%# 5. get consulted by ZuH4reZ? %>
<%# open chat with admin? %>
